"""Deterministic report generation for books entities.

Modules:
  cache      — regenerable SQLite cache (validation-gated, atomic swap)
  statements — P&L, balance sheet, trial balance, general ledger, and ``ask``
"""
